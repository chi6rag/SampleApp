<% if @feed_items.any? %>
  <ol class="microposts">
    <!-- render @feed_items renders micropost partial since @feed_items has a class mciropost -->
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>